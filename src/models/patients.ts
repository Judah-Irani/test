import { Injectable } from '@angular/core';

@Injectable()
export class Patients {
  age: string;
  dob: string;
  gender: string;
  is_verified: string;
  last_visit: string;
  lat: string;
  lng: string;
  mobile: string;
  mobile_code: string;
  mobile_flag: string;
  name: string;
  patient_id: string;
  photo: string;
}
